
import { atom } from 'jotai';

// Create favouritesAtom with the default value of an empty array
export const favouritesAtom = atom([]);

// Define the searchHistoryAtom with an empty array as the default value
export const searchHistoryAtom = atom([]);